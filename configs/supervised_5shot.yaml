data_root: './data'
fewshot_split: './data/splits/fewshot_5shot.json'
encoder_dim: 512
pretrained: false   # 不使用预训练
mode: 'full'        # 必须全微调（因为没有预训练）
batch_size: 64
epochs: 100
lr: 0.01
momentum: 0.9
weight_decay: 1e-4
num_workers: 4
save_dir: './experiments/supervised/5shot'